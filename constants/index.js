import contractAddesses from "./contractAddresses.json"
//const { contractAddesses } = require("./constants/contractAddresses.json")
import abi from "./abi.json"

//const { abi } = require("./constants/abi.json")

module.exports = {
    abi,
    contractAddesses,
}